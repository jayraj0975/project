# Notebook space

The core project is intentionally implemented as reproducible Python scripts in `src/` rather than hiding everything in one giant notebook. A notebook can be added later for presentation/EDA walkthroughs.
